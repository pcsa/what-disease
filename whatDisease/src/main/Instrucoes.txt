### Primeira execucao ###

# 1. No arquivo src/resources/hibernate.cfg.xml:
 
1.1 troque o preenchimento da tag <property name="connection.username">username</property> para o respectivo username do seu banco de dados

troque o preenchimento da tag <property name="connection.password">password</property> para a respectiva password do seu banco de dados

1.2 No preencimento da tag <property name="hbm2ddl.auto">create</property> certifique de que esta escrito create.

# 2. 

### Demais execucoes ###

# 1. No arquivo src/resources/hibernate.cfg.xml, troque o preenchimento da tag <property name="hbm2ddl.auto">create</property> de create para update